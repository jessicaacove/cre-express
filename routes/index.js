const express = require('express');
const router  = express.Router();

/* GET home page. */
// router.get('/', (req, res, next) => {
//   res.render('index');
// });

// Deleted home page routes because we want angular to handle home page
// Express should only have API routes

module.exports = router;
